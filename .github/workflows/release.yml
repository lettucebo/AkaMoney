name: Release

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  DOTNET_VERSION: '8.0.x'
  NODE_VERSION: '22.x'
  AZURE_FUNCTIONAPP_NAME: akamoney-api
  AZURE_FUNCTIONAPP_HOST: akamoney-api-egfyapdmavc6hwba.japaneast-01.azurewebsites.net
  AZURE_FUNCTIONAPP_PATH: ./src/AkaMoney.Functions
  AZURE_STATIC_WEB_APP_NAME: delightful-sea-03f16ac00
  FRONTEND_PATH: ./src/akamoney-frontend
  AZURE_WEBAPP_NAME: akamoney-admin # Add your Azure Web App name here

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: nuget-${{ runner.os }}-${{ hashFiles('**/packages.lock.json') }}
        restore-keys: |
          nuget-${{ runner.os }}-

    - name: Restore .NET dependencies
      run: dotnet restore ./src/AkaMoney.sln

    - name: Publish Function App
      run: dotnet publish ${{ env.AZURE_FUNCTIONAPP_PATH }} --configuration Release --output ./publish

    - name: Cache Node.js modules
      uses: actions/cache@v4
      with:
        path: ${{ env.FRONTEND_PATH }}/node_modules
        key: node-${{ runner.os }}-${{ hashFiles('${{ env.FRONTEND_PATH }}/package-lock.json') }}
        restore-keys: |
          node-${{ runner.os }}-

    - name: Install Frontend dependencies
      run: npm ci
      working-directory: ${{ env.FRONTEND_PATH }}

    - name: Lint Frontend
      run: npm run lint
      working-directory: ${{ env.FRONTEND_PATH }}
      
    - name: Build Frontend
      run: npm run build
      working-directory: ${{ env.FRONTEND_PATH }}
      env:
        VUE_APP_API_URL: https://${{ env.AZURE_FUNCTIONAPP_HOST }}
        VUE_APP_REDIRECT_URL: https://${{ env.AZURE_FUNCTIONAPP_HOST }}
        VUE_APP_CLIENT_ID: ${{ secrets.VUE_APP_CLIENT_ID }}
        VUE_APP_TENANT_ID: ${{ secrets.VUE_APP_TENANT_ID }}
        VUE_APP_API_CLIENT_ID: ${{ secrets.VUE_APP_API_CLIENT_ID }}
        
    # 將原始程式碼保存為構件，以供後續部署使用
    - name: Upload Function App publish artifact
      uses: actions/upload-artifact@v4
      with:
        name: function-app-publish
        path: ./publish

    - name: Upload Frontend artifact
      uses: actions/upload-artifact@v4
      with:
        name: frontend
        path: ${{ env.FRONTEND_PATH }}/dist

  deploy-function:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Download Function App publish artifact
      uses: actions/download-artifact@v4
      with:
        name: function-app-publish
        path: functionapp

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    # 啟用 Storage Account access key
    - name: Enable Storage Account access key
      run: |
        az storage account update --name ${{ secrets.AZURE_STORAGE_ACCOUNT }} --resource-group ${{ secrets.AZURE_RG }} --allow-shared-key-access true

    # 為 Flex Consumption 部署準備 zip 檔案 (需要特定目錄)
    - name: Create deployment package for Flex Consumption
      run: |
        cd functionapp
        # 創建 .azurefunctions 目錄存放主要程式
        mkdir -p .azurefunctions
        # 保留必要檔案在根目錄並建立正確結構
        # 1. 建立目錄結構
        mkdir -p .azurefunctions
        
        # 2. 建立正確的目錄結構
        # 移動所有檔案到.azurefunctions目錄，除了必要檔案
        find . -maxdepth 1 \( \
          -name 'host.json' -o \
          -name 'local.settings.json' -o \
          -name 'extensions.json' -o \
          -name 'bin' -o \
          -name 'function.json' -o \
          -name '.azurefunctions' -o \
          -name '.' \
        \) -prune -o -exec mv {} .azurefunctions/ \;

        # 3. 驗證必要檔案存在
        echo "Required files in root:"
        ls -la host.json local.settings.json extensions.json bin function.json
        echo ".azurefunctions content:"
        ls -la .azurefunctions/

        # 3. 驗證目錄結構和檔案內容
        echo "Root directory structure:"
        ls -lah
        echo "host.json content:"
        cat host.json
        echo "local.settings.json content:"
        cat local.settings.json
        echo "AzureFunctions directory content:"
        ls -la .azurefunctions/
        
        # 建立包含完整結構的 zip 檔案
        zip -r ../deployment.zip .azurefunctions host.json local.settings.json extensions.json
        cd ..

    # 使用 Azure CLI 部署到 Flex Consumption Function App
    - name: Deploy to Azure Function App (Flex Consumption)
      run: |
        az functionapp deployment source config-zip \
          --resource-group ${{ secrets.AZURE_RG }} \
          --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
          --src deployment.zip

  deploy-frontend:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Download Frontend artifact
      uses: actions/download-artifact@v4
      with:
        name: frontend
        path: frontend

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy Frontend to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        package: frontend
